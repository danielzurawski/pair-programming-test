function match(campaigns, placements) {
    const response = [];

    // TODO : Implement matching
    // Example below
    response.push({
        placementId: 1,
        campaignId: 1,
        creativeId: 1,
        approved: true
    })


    return response;
}

module.exports = {
    match
}
